# staticcheck configuration.
#
# SA1019 flags aws-sdk-go-v2's feature/s3/manager as deprecated in favour of
# feature/s3/transfermanager. That is a real migration with its own testing
# needs — the upload path is how every blob reaches S3 — and it is tracked
# separately rather than rushed through to silence a linter.
#
# Nothing else is excluded: the remaining checks all run.
checks = ["all", "-SA1019", "-ST1000", "-ST1003", "-ST1020", "-ST1021", "-ST1022"]
